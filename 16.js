// and or operators





let firstName= "aryan"
let age = 24
if (firstName[0]=== "a" && age>18)
{
    console.log("name started with a and age is above 18")
}
// here you can see that && means if both side is true then true otherwise false always.
// both of the expression should be true in &&.
let first= "Aryan"
let ag = 12
if (first[0]=== "a" || ag>18)
{
    console.log("name started with a or age is above 18")
}
else{
    console.log("inside else")
}
// atleast one of then is true then output of || expression will be true.
